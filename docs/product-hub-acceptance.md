# 产品承接工程验收（待人工确认）

## 当前结论

- **工程状态：本地全量验证通过。** 本分支新增“乐可点名”和“归个类”产品入口及独立产品页；`npm run verify`、生产预览和公开 Release 链接核验均已通过。
- **发布状态：未发布。** 未提交、未推送、未合并，也未触发 Cloudflare Pages 部署。
- **人工门禁：必须保留。** 页面内容、截图、下载地址与发布仓库由负责人确认后，才能进入发布流程。

## 页面与入口

- `/products/`：产品入口与既有工程项目分区展示。
- `/products/leke-picker/`：乐可点名 v1.1 产品页。
- `/products/leke-picker/app/`：乐可点名在线应用，由产品源码仓库按固定 base 构建后作为静态发行物接入官网；应用自身保持 `noindex, nofollow` 且不加载 Clarity。
- `/products/guigelei/`：归个类 v1.5 产品页。

## 下载与仓库边界

- 乐可点名只链接公开源码仓库 `lekeopen/leke-picker` v1.1.0 的三个固定 Release 安装包，并展示 SHA-256、大小和系统风险说明。
- 归个类源码继续保持私有；官网只面向未来的公开 `lekeopen/guigelei-releases` 发布仓库。
- `guigelei-releases` 未就绪前，DMG 按钮保持“即将开放”，不得提供候选包、本地路径或虚假 URL。
- 下载统计只汇总当前正式版本的安装包白名单；GitHub API 不可用时降级为“下载统计暂不可用”。

## 隐私与统计

- 乐可点名名单只在浏览器或桌面应用本机处理。
- 归个类不读取文件正文、不上传文件；移动前必须人工确认。
- 官网保留现有 Microsoft Clarity，并仅记录预定义的产品按钮事件名；不传递名单、文件名、文件路径或文件内容。
- 在线应用构建不得注入 Clarity 或其他官网行为回放脚本。

## SEO 与素材

- 两个产品页进入公开路由清单、sitemap 和关键词映射。
- 每页输出独立 canonical、1200×630 OG 图片、`SoftwareApplication` 和三级面包屑结构化数据。
- `/products/leke-picker/app/` 不进入 sitemap；在线应用自身应输出 `noindex, nofollow`。
- 每款产品使用四张当前版本、演示数据、已脱敏的真实界面图。

## 发布前人工检查

- [ ] 确认两页文案、系统要求、风险说明和真实界面截图。
- [x] 乐可点名在线应用构建已接入预定子路径，输出 `noindex, nofollow`，且无 Clarity。
- [x] 乐可点名已完成开源审计，以 Apache-2.0 在 `lekeopen/leke-picker` 公开源码，并正式发布 v1.1.0 Release。
- [x] `guigelei-releases` 已创建，v1.5.0 Release、DMG、SHA-256 和更新记录一致。
- [x] 已在本地生产预览检查桌面端、390×844 移动端、放大文本保护规则和外链行为；页面无横向溢出。
- [x] `npm run verify` 全部通过：167 项应用测试、95 项 SEO 运维测试、28 个索引路由。
- [ ] 人工明确授权提交、推送、合并和发布；授权不得从本次工程实现自动延续。

## 发布后只读巡检

- 检查产品入口、两个产品页、OG 图片、canonical、JSON-LD、sitemap 和未知路由。
- 检查三个乐可点名安装包链接和一个归个类正式 DMG 链接。
- 检查在线应用能离线运行、不会被 sitemap 收录且没有 Clarity 请求。
- 检查 Clarity 只出现白名单产品事件，不包含产品本地数据。

## 回滚

回滚到上一条已验证的生产提交并重新部署。产品页、产品素材、目录数据和 SEO 清单均为加法变更，不涉及数据库迁移。若只有某个安装包失效，先将对应下载项改为 `pending` 并重新完成工程与人工门禁，不在生产产物中直接修补。
