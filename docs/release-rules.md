# 发布流程规则

1. **开发分支**：所有内容修改和功能开发只在 `develop` 分支进行。
2. **本地验证**：提交前在本地预览 (`npm run dev`)，确认无误。
3. **合并上线**：
   - 将 `develop` 合并到 `main`。
   - 推送 `main` 分支触发自动部署（即上线）。
4. **记录日志**：在 `RELEASES.md` 中记录发布日志。
