# V1.4 搜索收录与 SEO 运营工程验收

## 验收结论

- **工程门禁：通过。** 本分支的确定性检查、构建、测试、SEO URL 清单、关键词校验和两种 provider dry-run 已通过。
- **生产巡检：待发布后复查。** 2026-08-11 的只读线上巡检为 40/41，通过之外仍有一个生产 404，不能标记为通过。
- **搜索平台验收：待人工操作。** 本次没有登录站长平台、验证站点、提交 sitemap、执行真实 URL 提交或声称页面已收录。

本文件只记录工程验收，不代表 V1.4 已发布，也不把“平台接受提交”视为“已建立索引”。

## 工程证据

执行环境：Node.js 24.16.0，2026-08-11（Asia/Shanghai）。

| 检查 | 结果 | 证据摘要 |
| --- | --- | --- |
| 策略测试 RED | 符合预期 | 接入前 `tests/seo-ops-workflow.test.mjs` 因缺少 `verify:seo-ops` 失败 |
| 策略测试 GREEN | 通过 | 3/3 通过 |
| `npm run verify:seo-ops` | 通过 | 26 个 canonical URL；关键词校验通过；45/45 SEO 运营测试通过 |
| `npm run verify` | 通过 | 内容、TypeScript、ESLint、生产构建、95/95 全量测试、26 路由 SEO 构建校验、SEO 运营门禁和 bundle budget 全部退出 0 |
| 百度 dry-run | 通过 | 26 个待提交 URL；状态为 `dry-run` |
| IndexNow dry-run | 通过 | 26 个待提交 URL；状态为 `dry-run` |
| dry-run 状态边界 | 通过 | 两次执行前后 `.seo-ops/state.json` 均不存在；没有 provider 请求或本地状态写入 |
| `git diff --check` | 通过 | 没有空白错误 |
| 生产只读巡检 | **待复查** | 40/41；`https://lekeopen.com/products/xiaole-agent` 的站内链接检查返回 404 |

全量构建只刷新了 `public/rss.xml` 的 `lastBuildDate`、`publish-queue.json` 的 `generatedAt` 和文件末尾换行；内容没有语义变化，因此这些生成噪声未纳入提交。

### 生产巡检说明

结构化巡检开始时间为 `2026-08-10T21:37:06.609Z`。唯一失败项：

```text
check: same-origin-link-status
url: https://lekeopen.com/products/xiaole-agent
expected: 2xx or 3xx
actual: 404
```

本分支已把对应内容链接改为 canonical `/projects/xiaole-agent`，但生产环境尚未部署本分支。发布后必须重新运行 `npm run seo:inspect`；只有得到 0 个失败项，才能完成生产巡检检查点。

## 运营命令与边界

### 确定性质量门禁

```bash
npm run verify
npm run verify:seo-ops
```

`verify:seo-ops` 只执行 URL inventory、关键词校验和本地 SEO 运营测试。CI 不运行在线生产巡检，不读取 provider 凭据，也不执行 URL 提交。

### 生产巡检

```bash
npm run seo:inspect
npm run seo:inspect -- --json
```

巡检只对 `https://lekeopen.com` 发起有界 GET 请求，最多 24 次。它是独立的只读运营命令，可能受生产发布状态和网络波动影响，因此不属于确定性 CI 门禁。

### URL 清单与提交

```bash
npm run seo:inventory
npm run seo:submit -- baidu --dry-run
npm run seo:submit -- indexnow --dry-run
```

省略 `--execute` 时默认为 dry-run；运营记录仍应显式写出 `--dry-run`。dry-run 只显示待提交 canonical URL，不调用 provider，也不更新 `.seo-ops/state.json`。

真实写入必须由负责人逐次授权，并把最后一个参数改为 `--execute`：

```bash
npm run seo:submit -- baidu --execute
npm run seo:submit -- indexnow --execute
```

| 环境变量 | 用途 | 保密边界 |
| --- | --- | --- |
| `BAIDU_SITE` | 百度提交站点 | 可公开站点值，但只在受控运营环境配置 |
| `BAIDU_SUBMIT_TOKEN` | 百度 URL 提交授权 | 私密；禁止进入仓库、CI 日志、截图、工单或报告 |
| `INDEXNOW_KEY` | IndexNow 所有权 key | 协议要求公开验证，但仍只在受控构建/运营环境管理 |
| `INDEXNOW_KEY_LOCATION` | 可选的 IndexNow key 文件 URL | 公开 URL |
| `BAIDU_SITE_VERIFICATION` | 百度 HTML meta 所有权证明 | 公开验证材料，生产构建变量 |
| `GOOGLE_SITE_VERIFICATION` | Google HTML meta 所有权证明 | 公开验证材料，生产构建变量 |
| `BING_SITE_VERIFICATION` | Bing HTML meta 所有权证明 | 公开验证材料，生产构建变量 |

成功接受的提交才会以脱敏记录写入 gitignored 的 `.seo-ops/state.json`，供后续计算待提交差集。不要提交该文件，也不要把删除它当作撤回或普通重试手段。

### 月度报告

```bash
npm run seo:report -- --input /absolute/path/sanitized-monthly-seo.json --output /absolute/path/monthly-seo-report.md
```

输入只能包含脱敏汇总值；缺失指标保留为 `null`。默认拒绝覆盖已有报告，人工确认后才可加 `--force`。模板见 `docs/seo/monthly-report-template.json`。

## 平台检查点

- [ ] V1.4 分支发布到生产；
- [ ] 发布后 `npm run seo:inspect` 为 0 个失败项；
- [ ] 人工完成百度搜索资源平台所有权验证；
- [ ] 人工完成 Google Search Console 所有权验证；
- [ ] 人工完成 Bing Webmaster Tools 所有权验证；
- [ ] 人工确认三个平台均接受 `https://lekeopen.com/sitemap.xml`；
- [ ] 取得单独授权后执行百度真实 URL 提交并保存脱敏结果；
- [ ] 取得单独授权后执行 IndexNow 真实 URL 提交并保存脱敏结果；
- [ ] 后续以平台覆盖率/索引报告确认核心页面已建立索引。

遇到登录、MFA、CAPTCHA、DNS 账户访问、Verify 按钮或 sitemap 确认时必须暂停，交由站点负责人操作。详细步骤见 `docs/seo/platform-setup.md`。

## 回滚与事故处理

1. **停止写入：** 不再使用 `--execute`；如果凭据疑似泄露，立即在 provider 端撤销或轮换，并清理受影响的受控环境。
2. **代码/发布回滚：** 回滚 V1.4 部署到上一已验证版本，再运行只读生产巡检。所有权证明变量的撤销需先确认不会破坏仍在使用的平台验证方式。
3. **状态恢复：** 修复 `.seo-ops/state.json` 前先在仓库外备份。只恢复已确认接受的脱敏记录；删除状态会让 URL 重新进入待提交集合。
4. **平台事实：** 已被 provider 接受的 URL 提交无法由本工具撤回；平台接受也不保证抓取或建立索引。
